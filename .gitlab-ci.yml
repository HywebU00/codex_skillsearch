stages:
  - build
  - deploy

cache:
  key: ${CI_COMMIT_REF_SLUG}-node22
  paths:
    - .npm/
    - node_modules/

build:
  stage: build
  image: node:22.13.0
  before_script:
    - node -v
    - npm -v
  script:
    - npm ci --cache .npm --prefer-offline
    - npm run build
  artifacts:
    when: always
    expire_in: 1 day
    paths:
      - dist
  only:
    - main

pages:
  stage: deploy
  image: node:22.13.0
  needs:
    - job: build
      artifacts: true
  before_script:
    - node -v
    - npm -v
  script:
    - rm -rf public
    - mkdir -p public
    - cp -r dist/* public/
  artifacts:
    paths:
      - public
  only:
    - main
